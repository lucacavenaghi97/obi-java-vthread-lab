# Diagnostic override: downstream closes the connection after every response.
services:
  downstream:
    volumes:
      - ./nginx-echo-close.conf:/etc/nginx/conf.d/default.conf:ro
      - ./tls:/etc/nginx/certs:ro
